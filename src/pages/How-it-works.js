import React, { Component } from "react";
import Title from "../components/Title";

export default class HowPage extends Component {
  render() {

    return (
      <div className="col-10">
        <Title name="How it works (in progress)" />
      </div>
    );
  }
}
